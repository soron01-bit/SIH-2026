# Frontend Environment Configuration
VITE_API_URL=http://localhost:8000/api

# AI Voice Assistant — ephemeral token endpoint (served by FastAPI backend or token-server.mjs)
# The real GEMINI_API_KEY lives ONLY on the backend (.env), never here.
VITE_VOICE_TOKEN_URL=/voice/token
